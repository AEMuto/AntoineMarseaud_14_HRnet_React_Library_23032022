import React from 'react'

type TableProps = {
    data:{}
}

const Table = (props:TableProps) => {
    return (
        <table>
            <th>Default Title</th>
        </table>
    )
}

export default Table